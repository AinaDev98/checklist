{% extends 'admin.html.twig' %}
{% form_theme formBoutiques 'bootstrap_5_layout.html.twig' %}

{% block title %}Boutiques{% endblock %}

{% block body %}
<div class="bg-dark pt-2 pb-1">
    <h4 class="text-white text-center">BOUTIQUES</h4>
</div>

    <div class="container mt-lg-5">
        <div class="text-center">
            <a href="{{ path('app_list_boutiques') }}" class="btn btn-default text-primary"><i class="fad fa-list"></i> Listes</a>
        </div>
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                {{ form(formBoutiques) }}
            </div>
        </div>
    </div>

{% endblock %}
